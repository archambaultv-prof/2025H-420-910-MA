---
sidebar_position: 5
sidebar_label: Variables, types et affectation
description: Variables, types et affectation
---

# Variables, types et affectation

## Qu'est-ce qu'une variable ?

Une variable est un nom qui permet de mémoriser une valeur dans un programme.
Elle agit comme une boîte étiquetée dans laquelle on range une donnée, qu’on
peut réutiliser ou modifier plus tard.

### Syntaxe de pseudo-code

On utilise souvent la syntaxe suivante pour déclarer une variable et lui
attribuer une valeur en pseudo-code :

```plaintext
nom_variable <- valeur
```

Par exemple, pour déclarer une variable `age` et lui attribuer la valeur `25`,
on écrirait :

```plaintext
age <- 25
```

Il est aussi possible, pour les besoins du cours, d'utiliser le symbole `=` à la place de `<-`.

```plaintext
age = 25
```

<details> 
<summary><strong>🤔 Testez votre compréhension</strong></summary>

 <MultipleChoiceTabs>
    <MultipleChoice>
    Quelles sont les variables dans le code suivant ?

    ```plaintext
    x <- 25
    WHILE x > 0 DO
        x <- x - 1
        PRINT x
        y <- x * 2
        PRINT (y - x)
    END WHILE
    ```

        <MultipleChoiceOption>

        `x`, `y`, `x * 2` et `x - 1`

        </MultipleChoiceOption>

        <MultipleChoiceOption isCorrect>

        `x` et `y`

        </MultipleChoiceOption>
        
        <MultipleChoiceOption>

        `x`, `y`, `WHILE`, `DO`, `END WHILE` et `PRINT`

        </MultipleChoiceOption>

    </MultipleChoice>
 </MultipleChoiceTabs>
 </details> 